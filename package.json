{
  "name": "linkedin-networker",
  "version": "1.0.0",
  "description": "LinkedIn networking application with QR code registration",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && CI=false npm install && CI=false npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "vercel-build": "cd frontend && CI=false npm install && CI=false npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "express": "^4.18.2",
    "mongoose": "^6.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  }
}
